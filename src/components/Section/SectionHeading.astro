---
interface Props {
    title: string;
    isBlue?: boolean;
}

const { title, isBlue = false } = Astro.props;
---

<h1 class={`text-[80px] font-shippori ${isBlue ? "text-primary-blue" : "text-primary-green"}`}>
    {title}
</h1>
