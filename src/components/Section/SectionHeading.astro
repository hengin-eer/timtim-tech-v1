---
interface Props {
    title: string;
    isBlue?: boolean;
}

const { title, isBlue = false } = Astro.props;
---

<h1 class={`text-[90px] sm:text-[120px] md:text-[150px] lg:text-[180px] font-shippori ${isBlue ? "text-primary-blue" : "text-primary-green"}`}>
    {title}
</h1>
