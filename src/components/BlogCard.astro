---
interface Props {
	title: string;
	slug: string;
	updatedAt: string;
	eyecatch: string;
}

const { title, slug, updatedAt, eyecatch } = Astro.props;
---

<a
	class="w-full md:max-w-[480px] flex flex-col justify-start gap-6"
	href={`/blog/${slug}`}
>
	<img
		class="w-full aspect-video object-cover rounded-xl"
		src={eyecatch}
		alt={title}
	/>
	<div>
		<p class="mb-2 text-sm">{updatedAt}</p>
		<h3 class="text-xl">{title}</h3>
	</div>
</a>
